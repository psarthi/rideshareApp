<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical">

        <include layout="@layout/interest_item" android:id="@+id/interest_item"/>

        <ImageView
            android:id="@+id/interest_selected"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_tick_circle"
            android:tint="@android:color/white"
            android:layout_alignTop="@id/interest_item"
            android:layout_alignEnd="@id/interest_item"
            android:layout_marginTop="@dimen/default_margin"
            android:layout_marginEnd="@dimen/default_margin"/>

</RelativeLayout>